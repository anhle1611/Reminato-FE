export type userState = {
    access_token: string,
    token_type: string,
    expires_in: number,
    refresh_token: string,
    created_at: string,
    uid: number,
    name: string,
    expires_on: string
};